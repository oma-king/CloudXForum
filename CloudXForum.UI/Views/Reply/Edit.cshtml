@model CloudXForum.UI.Models.Reply.EditPostReplyModel
@{
    ViewData["Title"] = "Edit";
}

<div class="container body-content">
    @if (User.Identity.Name == Model.AuthorName || User.IsInRole("Admin"))
    {
        <div class="container body-content">
            <div class="row sectionHeader">
                <div class="sectionHeading">
                    Edit message
                    <span id="headingBtn">
                        <a asp-controller="Post" asp-action="Index" asp-route-id="@Model.PostId" class="btn btn-back">Back to @Model.PostTitle</a>
                    </span>
                </div>
            </div>
            <div class="row">
                <div id="editReply">
                    <form asp-action="EditReply" method="post" id="addPostForum">
                        <div asp-validation-summary="All" class="text-danger">
                        </div>
                        <div class="form-group">
                            <label asp-for="Content">Message: </label>
                            <textarea asp-for="Content" rows="10" class="form-control" required placeholder="Write an answer..." value="@Model.Content" maxlength="300"></textarea>
                            <span asp-validation-for="Content" class="text-danger"></span>
                        </div>
                        <input asp-for="ReplyId" type="hidden"/>
                        <input asp-for="PostId" type="hidden"/>
                        <button type="submit" id="submitReplyBtn" class="btn btn-submitPost">Update Reply</button>
                    </form>
                </div>
            </div>
        </div>
    }
    else
    {
        <h1 class="text-danger">Error.</h1>
        <h2 class="text-danger">You dont have access to this profile</h2>
    }
</div>